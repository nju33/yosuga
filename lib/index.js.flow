export type Option = {
  base?: string,
  targets?: string[],

  accentColor?: string
  subColor?: string
  baseColor?: string
};

export type SectionItems = {
  css?: string,
  scss?: string,
  less?: string,
  stylus?: string
};

export type Section = {
  name: string,
  layout: 'palet' | 'block',
  title?: string,
  html?: string | null,
  items?: SectionItems
};
